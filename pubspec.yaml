name: form_ai
description: "On-device AI exercise pose coaching for Android and iOS."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.2

dependencies:
  camera: 0.12.1
  flutter:
    sdk: flutter
  flutter_riverpod: 3.4.3
  google_mlkit_pose_detection: 0.16.1
  hand_detection: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5

flutter:
  uses-material-design: true
